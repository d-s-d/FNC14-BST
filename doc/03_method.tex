\section{Optimization}

\mypar{Quantifying Locality}

\mypar{Removing Column Accesses} If we only consider memory accesses within the
inntermost loop, half of them are of stride $1$ and half of them of stride $n+1$. 

\mypar{Bottom-Up} asdf
\mypar{Partial Results (Swap innermost loops)}
\mypar{Compressed memory layout}
\mypar{Blocking}
\mypar{Vectorization}
\mypar{Scheduling}
\mypar{Alignment}


